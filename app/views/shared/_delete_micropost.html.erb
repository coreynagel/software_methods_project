<% if current_user?(object.user) %>
    <%= link_to "delete", object, class: "btn btn-danger btn-mini pull-right delete-btn", method: :delete,
                data: { confirm: "You sure?" },
                title: object.content %>
<% end %>